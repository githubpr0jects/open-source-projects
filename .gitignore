# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

























































































};  }    return false;    console.error('Error resetting impressions:', error.message);  } catch (error) {    return true;    fs.writeFileSync(IMPRESSIONS_FILE, JSON.stringify({}, null, 2), 'utf-8');  try {export const resetAllImpressions = () => { */ * Reset all impressions/**};  }    return 0;    console.error(`Error setting impressions for sponsor ${sponsorId}:`, error.message);  } catch (error) {    return data[sponsorId];    fs.writeFileSync(IMPRESSIONS_FILE, JSON.stringify(data, null, 2), 'utf-8');    data[sponsorId] = Math.max(0, count); // Ensure non-negative    const data = getImpressionsData();  try {export const setImpressions = (sponsorId, count) => { */ * Useful for resetting or bulk updates * Set impressions for a sponsor to a specific value/**};  }    return 0;    console.error(`Error incrementing impressions for sponsor ${sponsorId}:`, error.message);  } catch (error) {    return data[sponsorId];        fs.writeFileSync(IMPRESSIONS_FILE, JSON.stringify(data, null, 2), 'utf-8');    // Write back to file        data[sponsorId] += 1;    // Increment        }      data[sponsorId] = 0;    if (!(sponsorId in data)) {    // Initialize to 0 if sponsor not in file        const data = getImpressionsData();  try {export const incrementImpressions = (sponsorId) => { */ * Creates file if it doesn't exist * Increment impressions for a sponsor/**};  }    return 0;    console.warn(`Error getting impressions for sponsor ${sponsorId}:`, error.message);  } catch (error) {    return data[sponsorId] || 0;    const data = getImpressionsData();  try {export const getSponsorImpressions = (sponsorId) => { */ * Returns 0 if sponsor not found in file * Get impressions for a specific sponsor/**};  return {};  }    console.warn('Error reading impressions file:', error.message);  } catch (error) {    }      return JSON.parse(data);      const data = fs.readFileSync(IMPRESSIONS_FILE, 'utf-8');    if (fs.existsSync(IMPRESSIONS_FILE)) {  try {export const getImpressionsData = () => { */ * Returns empty object if file doesn't exist * Get all impressions from the file/**const IMPRESSIONS_FILE = path.join(process.cwd(), 'sponsors-impressions.json');# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
# Sponsor impressions data (local tracking, not part of version control)
sponsors-impressions.json